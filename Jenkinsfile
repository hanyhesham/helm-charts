pipeline {
    agent any
    stages {
        stage('Remove Docker Image') {
            steps{
              
                   echo 'hellopipeline: '+ env.current_status        
            }
            }
        }
  }
}
